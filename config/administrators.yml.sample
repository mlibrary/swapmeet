development:
  root:
    - root@example.com

test:
  root:
    - root@example.com

production:
  root:
    - <%= ENV["ROOT_ADMINISTRATOR"] %>
  application:
    - <%= ENV["APPLICATION_ADMINISTRATOR"] %>
  platform:
    - <%= ENV["PLATFORM_ADMINISTRATOR"] %>
